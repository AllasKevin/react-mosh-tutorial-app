import ListGroup from "./components/ListGroup";
// This is a component
function App() { // we always have to close our react component, we could also use the self closing syntax <ListGroup /> which is more consice.
  return <div><ListGroup></ListGroup></div>
}

export default App;