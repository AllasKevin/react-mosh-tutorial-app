import Message from "./Message"
// This is a component
function App() { // we always have to close our react component, we could also use the self closing syntax <Message /> which is more consice.
  return <div><Message></Message></div>
}

export default App;